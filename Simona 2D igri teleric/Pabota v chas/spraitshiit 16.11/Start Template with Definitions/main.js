'use skrict'
const Game = new Phaser.Game(800, 800, Phaser.AUTO, 'game-canvas', { preload, create })

function preload() {
    Game.load.spr

}

function create() {
    console.log('Hello there :)')

 
}